This is a simple script that runs at each startup of a Linux session, to set the scroll speed to a certain value.

The ID of the touchpad is identified and it's settings are changed for the appropriate parameter (scroll speed, here) to make the scrolling in Linux Mint slower.
